@model IntelliTest.Core.Models.Questions.Closed.ClosedQuestionAnswerViewModel

<div class="questBox">
    <div class="customRow">
        <div class="questTextBox">
            @if (Model.IsEquation)
            {
                <p class="math-field question">@Model.Text</p>
            }
            else
            {
                <p>@Model.Text</p>
            }
        </div>
    </div>
    <div class="choice" onFocus="onFocus(this)" onBlur="onFocusOut(this)">
        @for (int i = 0; i < Model.PossibleAnswers.Count(); i++)
        {
            <div class="questTextBox option">
                <div>
                    <input type="checkbox" asp-for="@Model.Answers[i]" value="@Model.Answers[i]"/>
                </div>
                @if (Model.IsEquation)
                {
                    <p class="math-field text">@Model.PossibleAnswers[i]</p>
                }
                else
                {
                    <p>@Model.PossibleAnswers[i]</p>
                }
            </div>
        }
    </div>
</div>